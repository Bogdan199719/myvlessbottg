# MyVlessBot - Архитектура и Полное Руководство

## 📋 Содержание
1. [Общий Обзор](#общий-обзор)
2. [Структура Проекта](#структура-проекта)
3. [Ключевые Модули](#ключевые-модули)
4. [Жизненный Цикл Приложения](#жизненный-цикл-приложения)
5. [Базы Данных](#базы-данных)
6. [Система Платежей](#система-платежей)
7. [API и Webhook](#api-и-webhook)
8. [Логирование и Обработка Ошибок](#логирование-и-обработка-ошибок)
9. [Развертывание и DevOps](#развертывание-и-devops)
10. [Потенциальные Проблемы и Решения](#потенциальные-проблемы-и-решения)

---

## 🎯 Общий Обзор

**MyVlessBot** — это автоматизированная торговая платформа для продажи VLESS VPN-конфигураций через Telegram. Бот интегрирован с панелью управления **3x-ui** и предоставляет:

- **Автоматическое выделение ключей** после оплаты
- **Веб-админ-панель** для управления тарифами и серверами
- **Многосерверная архитектура** с поддержкой неограниченного числа хостов
- **Гибкая система тарификации** (месячные, годовые, пробные периоды)
- **Множество платежных шлюзов**: YooKassa, CryptoBot, Heleket, TonConnect, Telegram Stars
- **Встроенная система поддержки** с тикет-системой
- **Реферальная система** для привлечения пользователей

### Основные Характеристики
- **Python 3.10** + **Aiogram 3.21** (асинхронная библиотека для Telegram)
- **Flask 3.1** для веб-панели и вебхуков
- **SQLite3** для хранения данных
- **Docker** для развертывания
- **py3xui** для управления 3x-ui панелью

---

## 📁 Структура Проекта

```
vless-shopbot/
├── src/shop_bot/                          # Основной пакет приложения
│   ├── __main__.py                        # Точка входа, инициализация сервисов
│   ├── __init__.py
│   ├── bot_controller.py                  # Управление ботами (shop_bot и support_bot)
│   ├── config.py                          # Шаблоны текстов сообщений
│   ├── version.py                         # Версия приложения
│   │
│   ├── bot/                               # Telegram бот логика
│   │   ├── handlers.py                    # Основные обработчики команд и кнопок (~2700 строк)
│   │   ├── support_handlers.py            # Обработчики для бота поддержки
│   │   ├── keyboards.py                   # Клавиатуры и кнопки
│   │   └── middlewares.py                 # Middleware (проверка блокировок пользователей)
│   │
│   ├── data_manager/                      # Работа с данными
│   │   ├── database.py                    # SQLite операции (~1300 строк)
│   │   ├── database_helpers.py            # Вспомогательные функции БД
│   │   └── scheduler.py                   # Планировщик уведомлений о подписках
│   │
│   ├── modules/
│   │   └── xui_api.py                     # Интеграция с 3x-ui панелью (~600 строк)
│   │
│   ├── utils/
│   │   ├── time_utils.py                  # Работа с временем (MSK timezone)
│   │   └── update_manager.py              # Проверка и применение обновлений
│   │
│   └── webhook_server/                    # Flask веб-приложение
│       ├── app.py                         # Основное Flask приложение (~1300 строк)
│       ├── subscription_api.py            # API для мобильных клиентов (~230 строк)
│       ├── issue_key_snippet.py           # Вспомогательные функции
│       ├── templates/                     # HTML шаблоны админ-панели
│       │   ├── base.html
│       │   ├── dashboard.html
│       │   ├── settings.html
│       │   ├── keys.html
│       │   ├── users.html
│       │   └── ... (ещё 3 шаблона)
│       └── static/                        # CSS и JavaScript
│           ├── css/style.css
│           └── js/script.js
│
├── docker-compose.yml                     # Docker конфигурация
├── Dockerfile                             # Образ контейнера
├── pyproject.toml                         # Зависимости проекта
├── README.md                              # Основная документация
├── migration_guide.md                     # Инструкция по миграции
└── install.sh                             # Автоматический скрипт установки
```

---

## 🔌 Ключевые Модули

### 1. **bot_controller.py** - Управление Ботами
**Назначение**: Контроллер для управления двумя независимыми Telegram ботами.

**Ключевые компоненты**:
- `BotController` класс с методами:
  - `start_shop_bot()` — запуск основного бота продаж
  - `start_support_bot()` — запуск бота поддержки
  - `stop()` — остановка обоих ботов
  - `get_status()` — получение статуса ботов

**Управление платежами**:
- Инициализирует YooKassa конфигурацию
- Проверяет доступность всех платежных методов
- Передает конфигурацию в handlers

**Обработка сигналов**:
- Перехватывает SIGINT и SIGTERM для корректного завершения
- Отменяет все активные задачи при выключении

### 2. **handlers.py** - Основная Логика Бота (~2700 строк)
**Назначение**: Все обработчики команд, кнопок и платежей.

**Основные команды**:
- `/start` — приветствие и регистрация пользователя
- `/profile` — профиль пользователя с историей покупок
- `/buy` — покупка новой подписки
- `/keys` — управление ключами VPN
- `/referral` — реферальная система
- `/support` — связь с поддержкой
- `/admin` — админ-панель (только для администратора)

**Состояния FSM (Finite State Machine)**:
- `KeyPurchase` — выбор хоста и тарифа
- `Onboarding` — согласие с условиями
- `PaymentProcess` — обработка платежа

**Платежные системы**:
- **YooKassa** — российский платежный шлюз
- **Telegram Stars** — встроенная система платежей Telegram
- **CryptoBot** — криптовалютные платежи
- **Heleket** — альтернативный платежный шлюз
- **TonConnect** — платежи в TON (крипто)

### 3. **database.py** - SQLite Управление (~1300 строк)
**Назначение**: Все операции с базой данных.

**Основные таблицы**:

| Таблица | Назначение |
|---------|-----------|
| `users` | Пользователи, балансы, статусы блокировки |
| `vpn_keys` | Выданные VPN ключи, сроки действия |
| `transactions` | История платежей с ID и методами |
| `bot_settings` | Конфигурация (токены, домены, пароли) |
| `plans` | Тарифные планы (хост, месяцы, цена) |
| `xui_hosts` | 3x-ui панели с учетными данными |
| `support_threads` | Связь пользователей с тикетами поддержки |
| `vpn_keys_missing` | Потеряные ключи (для отладки) |

**Критичные функции**:
- `initialize_db()` — создание таблиц и начальные значения
- `register_user_if_not_exists()` — регистрация нового пользователя
- `get_user()` — получение данных пользователя
- `add_new_key()` — добавление купленного ключа
- `log_transaction()` — логирование платежа
- `run_migration()` — миграция при обновлениях

**Миграции**:
- Автоматически добавляет новые колонки при обновлении
- Генерирует уникальные subscription_token для каждого пользователя

### 4. **xui_api.py** - Интеграция с 3x-ui (~600 строк)
**Назначение**: Управление VPN ключами через панель 3x-ui.

**Основные функции**:
- `create_or_update_key_on_host()` — создание/обновление ключа на хосте
- `create_or_update_key_on_host_seconds()` — создание ключа с указанием секунд действия
- `get_client_stats()` — получение статистики использования ключа
- `get_country_flag_by_host()` — определение флага страны по имени хоста

**Особенности**:
- Использует библиотеку `py3xui` для API панели
- Поддерживает множество хостов одновременно
- Логирует ошибки с rate-limiting (один раз в 5 минут на одну ошибку)
- Генерирует VLESS конфигурационные строки

### 5. **scheduler.py** - Планировщик Уведомлений
**Назначение**: Периодическая проверка и отправка уведомлений о подписках.

**Сценарии**:
- За 24 часа до истечения
- За 1 час до истечения
- В момент истечения (0 часов)
- После истечения (-24 часа) — напоминание

**Поддерживаемые типы**:
- Обычные пробные подписки
- Платные подписки
- Глобальные подписки (на все серверы)

**Проверка каждые 5 минут** (CHECK_INTERVAL_SECONDS = 300)

### 6. **app.py** (Flask) - Веб-Панель (~1300 строк)
**Назначение**: Админ-панель для управления ботом и данными.

**Основные страницы**:
- `/` (dashboard) — главная страница со статистикой
- `/login` — авторизация
- `/settings` — конфигурация (токены, домены, платежные шлюзы)
- `/keys` — управление VPN ключами
- `/users` — список пользователей, блокировка/разблокировка
- `/updates` — проверка и применение обновлений
- `/backup` — создание и восстановление резервных копий

**API endpoints**:
- `/api/hosts` — управление хостами (CRUD)
- `/api/plans` — управление тарифами (CRUD)
- `/api/transactions` — история платежей
- `/api/stats` — статистика и графики

**Безопасность**:
- Сессионная авторизация
- CSRF защита
- Валидация входных данных

### 7. **subscription_api.py** - API для Мобильных Клиентов (~230 строк)
**Назначение**: REST API для клиентских приложений (например, мобильные VPN приложения).

**Endpoints**:
- `GET /sub/<token>` — получить все активные ключи пользователя в формате конфигов
- Автоматическое создание глобальных ключей при наличии глобальной подписки

**Особенности**:
- Использует subscription_token вместо telegram_id
- Фильтрует отключенные хосты
- Дедублицирует ключи по хосту (берет самый свежий)
- Поддерживает экспортирование в клиентские приложения

---

## 🔄 Жизненный Цикл Приложения

### Запуск
1. **`__main__.py`** запускает `main()`
2. Инициализируется логирование
3. Инициализируется база данных (`database.initialize_db()`)
4. Создается `BotController()` и `Flask приложение`
5. Flask запускается в отдельном потоке на порту 1488
6. Запускается asyncio event loop
7. Периодическая проверка подписок в фоне (`periodic_subscription_check()`)
8. **Бот готов к запуску из веб-панели**

### Во время работы
- **Telegram события** → обрабатываются в `handlers.py`
- **Платежи** → webhook'и от платежных систем → обновляют БД
- **Проверка подписок** → каждые 5 минут → отправка уведомлений
- **Вебзапросы** → админ-панель на Flask

### Завершение
- Перехватываются сигналы SIGINT/SIGTERM
- Ботсы корректно остаются (await dp.stop_polling)
- Отменяются все активные asyncio задачи
- Приложение завершается

---

## 💾 Базы Данных

### SQLite структура

**users таблица**:
```sql
CREATE TABLE users (
    telegram_id INTEGER PRIMARY KEY,
    username TEXT,
    total_spent REAL DEFAULT 0,           -- Всего потрачено в рублях
    total_months INTEGER DEFAULT 0,       -- Всего куплено месяцев
    trial_used BOOLEAN DEFAULT 0,         -- Пробный период использован?
    agreed_to_terms BOOLEAN DEFAULT 0,    -- Согласие с условиями
    registration_date TIMESTAMP,
    is_banned BOOLEAN DEFAULT 0,          -- Заблокирован ли пользователь?
    referred_by INTEGER,                  -- Кто его рекомендовал
    referral_balance REAL DEFAULT 0,      -- Баланс рефереров в рублях
    referral_balance_all REAL DEFAULT 0,  -- Общий баланс рефереров
    subscription_token TEXT UNIQUE        -- Токен для мобильных клиентов
)
```

**vpn_keys таблица**:
```sql
CREATE TABLE vpn_keys (
    key_id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    host_name TEXT NOT NULL,              -- Имя хоста (кой сервер)
    xui_client_uuid TEXT NOT NULL,        -- UUID в панели 3x-ui
    key_email TEXT NOT NULL UNIQUE,       -- Email ключа (идентификатор)
    expiry_date TIMESTAMP,                -- Дата истечения
    created_date TIMESTAMP,               -- Дата создания
    connection_string TEXT                -- VLESS конфигурация
)
```

**transactions таблица**:
```sql
CREATE TABLE transactions (
    transaction_id INTEGER PRIMARY KEY AUTOINCREMENT,
    payment_id TEXT UNIQUE NOT NULL,      -- ID платежа в системе
    user_id INTEGER NOT NULL,
    status TEXT NOT NULL,                 -- succeeded, pending, failed
    amount_rub REAL NOT NULL,             -- Сумма в рублях
    amount_currency REAL,                 -- Сумма в иностранной валюте
    currency_name TEXT,                   -- Название валюты (USD, EUR и т.д.)
    payment_method TEXT,                  -- Метод платежа
    metadata TEXT,                        -- JSON с доп. информацией
    created_date TIMESTAMP
)
```

**Резервное копирование**:
- Веб-панель позволяет создавать резервные копии в ZIP
- ZIP содержит БД, метаданные (версия схемы, контрольная сумма) и опционально `.env`
- Восстановление проверяет контрольную сумму перед применением

---

## 💳 Система Платежей

### Поддерживаемые методы

1. **YooKassa** (российский платежный шлюз)
   - Требует: `YOOKASSA_SHOP_ID`, `YOOKASSA_SECRET_KEY`
   - Поддерживает СБП (система быстрых платежей)
   - Обработка вебхука в `handlers.py`

2. **Telegram Stars** (встроенная система Telegram)
   - Требует: включение в `stars_enabled`
   - Автоматический курс: 1.6 RUB за 1 звезду
   - Поддерживает переменный курс (через `stars_rub_per_star`)

3. **CryptoBot** (криптовалютные платежи)
   - Требует: `CRYPTOBOT_TOKEN`
   - Вебхук обрабатывает переводы в BTC, USDT и т.д.
   - Конвертация в RUB по курсу на момент платежа

4. **Heleket** (альтернативный платежный шлюз)
   - Требует: `HELEKET_MERCHANT_ID`, `HELEKET_API_KEY`
   - Поддерживает различные методы оплаты

5. **TonConnect** (блокчейн платежи)
   - Требует: `TON_WALLET_ADDRESS`, `TONAPI_KEY`
   - Мониторит блокчейн для подтверждения платежей
   - Требует создания QR кода для сканирования

### Поток платежа
1. Пользователь выбирает тариф
2. Бот генерирует счет и передает его платежной системе
3. Платежная система отправляет вебхук на `https://yourdomain.com/pay/...`
4. Обработчик вебхука обновляет статус платежа
5. При успешном платеже вызывается `issue_key()` для выделения ключа
6. Пользователь получает уведомление с ключом в Telegram

---

## 🌐 API и Webhook

### Webhook endpoints в Flask

| Path | Метод | Назначение |
|------|-------|-----------|
| `/pay/yookassa` | POST | Вебхук YooKassa платежей |
| `/pay/cryptobot` | POST | Вебхук CryptoBot платежей |
| `/pay/heleket` | POST | Вебхук Heleket платежей |
| `/pay/tonconnect` | POST | Обновления статуса TON платежей |
| `/sub/<token>` | GET | REST API для мобильных клиентов |

### Admin endpoints (требуют авторизации)

| Path | Метод | Назначение |
|------|-------|-----------|
| `/api/hosts` | GET, POST, DELETE | CRUD для хостов |
| `/api/plans/<host>` | GET, POST, DELETE | CRUD для тарифов |
| `/api/transactions` | GET | История платежей |
| `/api/stats` | GET | Статистика и графики |
| `/api/backup` | POST, GET | Резервная копия/восстановление |
| `/api/update-check` | GET | Проверка обновлений |

---

## 📊 Логирование и Обработка Ошибок

### Логирование
- **Уровень**: INFO по умолчанию
- **Формат**: `%(asctime)s - [%(levelname)s] - %(name)s - (%(filename)s).%(funcName)s(%(lineno)d) - %(message)s`
- **Подавленные библиотеки**: urllib3, requests, httpx (уровень WARNING)

### Обработка ошибок

**В bot_controller.py**:
```python
try:
    await dp.start_polling(bot)
except asyncio.CancelledError:
    logger.info("Polling was cancelled")
except Exception as e:
    logger.error(f"Error during polling: {e}", exc_info=True)
```

**В xui_api.py**:
- Rate-limiting ошибок (одна и та же ошибка логируется максимум раз в 5 минут)
- Безопасная обработка недостижимых хостов

**В handlers.py**:
- Try-except блоки вокруг операций с Telegram API
- Обработка `TelegramBadRequest` для недействительных данных
- Graceful fallback при недоступности платежных систем

**В database.py**:
- Try-except при создании таблиц
- Миграция с проверкой существования колонок

### Особенности обработки сбоев

1. **Потеря ключа на хосте** → записывается в `vpn_keys_missing` для отладки
2. **Хост недоступен** → логируется, но платеж не отменяется
3. **Платеж зависал** → admin может вручную переиздать ключ через панель
4. **Бот упал** → docker-compose перезапустит контейнер (restart: unless-stopped)

---

## 🐳 Развертывание и DevOps

### Docker Setup

**Dockerfile**:
- Base: `python:3.10-slim`
- Installs: gcc, docker.io, git
- Sets: `PYTHONUNBUFFERED=1` для вывода логов в реальном времени
- Mounts: volume для hot-reload изменений кода

**docker-compose.yml**:
- Сервис `bot` слушает на порту 1488
- Environment: TZ=Europe/Moscow (для корректного времени)
- Логирование: json-file driver с ротацией (max 10MB на файл)
- Restart: unless-stopped (автоматический перезапуск при падении)

### Процесс обновления на сервере

1. **Локально**: редактируем файлы в папке `src/`
2. **На сервере**:
   ```bash
   docker-compose down
   # Заменяем папку src локальной версией
   docker-compose up -d
   ```
3. **Важно**: Не трогаем `users.db` и `.env` при копировании!

**Если изменяем файлы вне src**:
- `Dockerfile` → пересобрать образ: `docker-compose up -d --build`
- `docker-compose.yml` → остановить и заново запустить
- `pyproject.toml` → переустановить зависимости в контейнере

### Файлы на сервере, требующие внимания

| Файл | Назначение | Копировать? |
|------|-----------|-----------|
| `users.db` | БД с данными пользователей | ❌ Никогда! |
| `.env` | Конфигурация и токены | ⚠️ Осторожно! |
| `src/` | Исходный код | ✅ Да |
| `docker-compose.yml` | Docker конфиг | ✅ Да |
| `Dockerfile` | Docker образ | ✅ Да |
| `pyproject.toml` | Зависимости | ✅ Да |

---

## 🐛 Потенциальные Проблемы и Решения

### 1. Ошибка: "Бот не отвечает на команды"

**Возможные причины**:
- Бот не запущен из админ-панели
- Токен неверный
- Telegram API недоступна

**Решение**:
1. Проверьте статус бота в админ-панели
2. Перезагрузите бот: остановите и запустите заново
3. Проверьте токен в settings
4. Посмотрите логи: `docker logs <container_id>`

### 2. Ошибка: "Ключ не выдан после платежа"

**Возможные причины**:
- Платежная система не отправила вебхук
- Хост 3x-ui недоступен
- Email ключа уже существует в БД

**Решение**:
1. Проверьте статус платежа в админ-панели (страница Transactions)
2. Попробуйте вручную переиздать ключ на странице Keys
3. Проверьте доступность хостов в Settings → Hosts
4. Проверьте логи 3x-ui панели

### 3. Ошибка: "Cronjob уведомлений не работает"

**Возможные причины**:
- Планировщик остановился при ошибке
- БД заблокирована

**Решение**:
1. Перезагрузите контейнер: `docker-compose restart bot`
2. Проверьте размер БД (может быть слишком большой)
3. Запустите `VACUUM` на БД для дефрагментации

### 4. Ошибка: "Не удается подключиться к хосту"

**Возможные причины**:
- Хост 3x-ui неверно сконфигурирован
- Сеть контейнера не может достичь хоста
- IP адрес поменялся

**Решение**:
1. Проверьте в админ-панели Settings → Hosts что URL верный
2. Используйте FQDN вместо IP если возможно
3. Проверьте доступность: `curl -u admin:password https://host:port`

### 5. Ошибка: "Вебхук платежа не получен"

**Возможные причины**:
- Домен неправильно сконфигурирован
- Firewall блокирует входящие запросы
- SSL сертификат невалиден

**Решение**:
1. Убедитесь что домен в settings совпадает с реальным доменом сервера
2. Проверьте что на сервере открыт порт 1488 (или тот, что используется)
3. Тестируйте вебхук вручную: `curl -X POST https://domain.com/pay/yookassa -d '...'`
4. Проверьте SSL сертификат: `openssl s_client -connect domain.com:443`

### 6. Проблема: "БД повреждена (Database is locked)"

**Решение**:
1. Остановите бот: `docker-compose down`
2. Проверьте БД: `sqlite3 users.db "PRAGMA integrity_check;"`
3. При ошибке восстановите из резервной копии
4. Запустите снова: `docker-compose up -d`

### 7. Проблема: "Глобальная подписка не работает"

**Возможные причины**:
- Нет тарифов на "ALL" хост
- `plan_id` не выставлен для ключей

**Решение**:
1. Создайте тариф на хост "ALL" в админ-панели
2. Нужно обновить логику в `subscription_api.py` если ключи старые

### 8. Обновление зависает или падает

**Решение**:
1. Не запускайте обновление во время активных платежей
2. Проверьте место на диске: `df -h`
3. Если обновление сломалось, восстановите из резервной копии

---

## 🔧 Рекомендации по Разработке

### Добавление нового платежного метода

1. Создайте функцию-обработчик в `handlers.py`
2. Добавьте новый вебхук endpoint в `app.py`
3. Добавьте проверку включения в `get_active_payment_methods()`
4. Добавьте конфигурацию в `database.py` (в `default_settings`)
5. Добавьте UI для конфигурации в админ-панели

### Добавление нового типа подписки

1. Добавьте логику в `scheduler.py`
2. Обновите `subscription_api.py` если это влияет на выдачу ключей
3. Добавьте миграцию в `database.py` если нужны новые колонки

### Развертывание на продакшене

1. **Используйте SSL/TLS** (Let's Encrypt + Certbot)
2. **Используйте .env файл** с переменными окружения
3. **Создавайте регулярные резервные копии** БД
4. **Ограничивайте доступ к админ-панели** (основной домен или VPN)
5. **Используйте reverse proxy** (Nginx) для добавления безопасности
6. **Следите за логами** на предмет ошибок
7. **Тестируйте обновления** на staging перед production

---

## 📞 Контакты и Ссылки

- **GitHub**: https://github.com/Bogdan199719/myvlessbottg
- **Автор**: Bogdan199719
- **Email**: bogdan123456789101@gmail.com
- **Лицензия**: Все права защищены © 2024

---

**Последнее обновление**: 5 февраля 2026
**Версия проекта**: 2.3.7
