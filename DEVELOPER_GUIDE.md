# 🚀 Быстрая Справка Разработчика MyVlessBot

> Этот документ содержит основные команды, структуру и советы для быстрой работы с проектом.

---

## 📋 Быстрый Чек-лист для Разработки

### Перед началом сеанса
- [ ] Прочитал изменения в последних коммитах
- [ ] Проверил наличие `.env` файла на сервере
- [ ] Знаю пароль от админ-панели

### При добавлении нового функционала
- [ ] Добавил логирование для отладки
- [ ] Обработал исключения (try-except)
- [ ] Обновил документацию если нужно
- [ ] Протестировал на тестовом боте перед продакшеном

### При развертывании на сервере
- [ ] Сделал резервную копию БД
- [ ] Заменил `src/` папку локальной версией
- [ ] **НЕ трогал** файлы вне `src/` если не знаю что делаю
- [ ] Проверил что бот запустился: `docker logs`

---

## 🐳 Docker команды

```bash
# Запуск контейнеров
docker-compose up -d

# Остановка контейнеров
docker-compose down

# Просмотр логов
docker logs <container_name>
docker logs -f <container_name>  # Follow mode (реал-тайм)

# Перезагрузка
docker-compose restart

# Пересборка образа
docker-compose up -d --build

# Заход в контейнер
docker exec -it <container_name> bash
```

---

## 💾 Быстрые SQL запросы для отладки

```bash
# Заход в SQLite БД внутри контейнера
docker exec -it <container_name> sqlite3 users.db

# Внутри sqlite3:

# Сколько пользователей зарегистрировано
SELECT COUNT(*) FROM users;

# Всех пользователей и их балансы
SELECT telegram_id, username, total_spent, is_banned FROM users;

# Последние 10 платежей
SELECT transaction_id, user_id, status, amount_rub, created_date FROM transactions ORDER BY transaction_id DESC LIMIT 10;

# Все активные ключи пользователя (ID=12345)
SELECT key_id, host_name, key_email, expiry_date FROM vpn_keys WHERE user_id = 12345;

# Все хосты в системе
SELECT host_name, host_url FROM xui_hosts;

# Все тарифы для хоста "ALL"
SELECT plan_id, plan_name, months, price FROM plans WHERE host_name = "ALL";

# Выход
.exit
```

---

## 📁 Где искать функции

| Что нужно найти | Где искать |
|---|---|
| Обработка команды `/start` | `src/shop_bot/bot/handlers.py` (~150 строка) |
| Обработка кнопки "Купить" | `src/shop_bot/bot/handlers.py` (~800 строка) |
| Вебхук платежа YooKassa | `src/shop_bot/bot/handlers.py` (~1200 строка) |
| Создание ключа на хосте | `src/shop_bot/modules/xui_api.py` (~200 строка) |
| Проверка подписок | `src/shop_bot/data_manager/scheduler.py` (~50 строка) |
| Админ-панель dashboard | `src/shop_bot/webhook_server/app.py` (~200 строка) |
| API для мобильных клиентов | `src/shop_bot/webhook_server/subscription_api.py` (~1 строка) |
| Инициализация БД | `src/shop_bot/data_manager/database.py` (~15 строка) |

---

## 🔍 Отладка Частых Проблем

### Проблема: "Бот не получает обновления от Telegram"

```bash
# 1. Проверить статус бота в логах
docker logs <container_name> | grep -i "polling"

# 2. Проверить токен
docker logs <container_name> | grep -i "token"

# 3. Проверить интернет соединение внутри контейнера
docker exec <container_name> curl https://api.telegram.org/botXXX/getMe
```

### Проблема: "Платежный вебхук не обработан"

```bash
# 1. Проверить есть ли запрос в логах
docker logs <container_name> | grep -i "pay/"

# 2. Проверить статус платежа в БД
sqlite3 users.db "SELECT payment_id, status, created_date FROM transactions WHERE payment_id='XXX';"

# 3. Если статус "pending", попробовать вручную переиздать ключ через админ-панель
```

### Проблема: "Ключ на хосте не создался"

```bash
# 1. Проверить доступность хоста с сервера
docker exec <container_name> curl -u admin:password https://host:port/api/inbound

# 2. Проверить в БД что ключ есть в таблице
sqlite3 users.db "SELECT * FROM vpn_keys WHERE user_id=12345;"

# 3. Если ключ есть в БД но не на хосте, это значит он был потерян
# Удалите ключ из БД и переиздайте через админ-панель
sqlite3 users.db "DELETE FROM vpn_keys WHERE key_id=123;"
```

---

## 📝 Шаблон для логирования при добавлении нового кода

```python
import logging

logger = logging.getLogger(__name__)

async def my_new_function():
    try:
        logger.info("Starting my_new_function with params: xxx")
        # Ваш код здесь
        result = do_something()
        logger.info(f"Successfully completed my_new_function, result: {result}")
        return result
    except ValueError as e:
        logger.error(f"ValueError in my_new_function: {e}", exc_info=True)
        raise
    except Exception as e:
        logger.error(f"Unexpected error in my_new_function: {e}", exc_info=True)
        return None
```

---

## 🔄 Жизненный Цикл Платежа (для понимания flow)

```
1. Пользователь нажимает "Купить подписку"
   ↓
2. Бот отправляет ссылку на платежную систему
   ↓
3. Пользователь платит
   ↓
4. Платежная система отправляет вебхук на наш сервер
   ↓
5. Обработчик вебхука в handlers.py получает платеж
   ↓
6. Функция issue_key() вызывает xui_api.create_key()
   ↓
7. Ключ создается на хосте через 3x-ui API
   ↓
8. Ключ записывается в БД (таблица vpn_keys)
   ↓
9. Пользователь получает уведомление с ключом в Telegram
```

---

## 🎯 Основные Функции и Что Они Делают

### В handlers.py

```python
async def handle_start(message: types.Message):
    # Регистрирует пользователя, показывает главное меню

async def handle_buy_key(callback_query: types.CallbackQuery):
    # Начинает процесс покупки нового ключа

async def handle_payment_yookassa(request):
    # Обрабатывает вебхук платежа от YooKassa

async def issue_key(user_id, host_name, plan_id):
    # Главная функция выделения ключа пользователю
    # 1. Проверяет что пользователь не превышает лимит
    # 2. Вызывает xui_api.create_or_update_key_on_host()
    # 3. Записывает ключ в БД
    # 4. Отправляет уведомление пользователю
```

### В xui_api.py

```python
async def create_or_update_key_on_host(host_name, email, months):
    # Создает или обновляет ключ на хосте 3x-ui
    # Возвращает словарь с информацией о ключе

def get_country_flag_by_host(host_name):
    # Определяет флаг страны по имени хоста
    # Используется для красивого отображения
```

### В scheduler.py

```python
async def periodic_subscription_check(bot_controller):
    # Бесконечный цикл каждые 5 минут:
    # 1. Получает все ключи из БД
    # 2. Проверяет когда они истекают
    # 3. Отправляет напоминания за 24ч, 1ч, и в момент истечения
```

### В database.py

```python
def get_user(user_id):
    # Возвращает все данные пользователя

def add_new_key(user_id, host_name, xui_client_uuid, key_email, expiry_timestamp_ms):
    # Добавляет новый ключ в БД

def log_transaction(user_id, payment_id, status, amount_rub, payment_method, metadata):
    # Логирует платеж в БД

def get_setting(key):
    # Получает значение из таблицы bot_settings
```

---

## 🚨 Критичные Моменты (На Что Обратить Внимание)

1. **Никогда не удаляйте `users.db` без резервной копии!**
   - В этом файле все данные пользователей

2. **Время в проекте в MSK timezone**
   - Используйте `time_utils.get_msk_now()` вместо обычного `datetime.now()`

3. **Все платежи логируются в `transactions` таблице**
   - Если платеж не зарегистрирован, это критичная ошибка

4. **FSM (Finite State Machine) в handlers.py для сложных потоков**
   - Если добавляете новые команды с выбором, используйте StatesGroup

5. **Вебхуки должны быть synchronous** во Flask
   - Но внутри они могут вызывать async функции через `asyncio.run()`

6. **Глобальные переменные в handlers.py** (ADMIN_ID, TELEGRAM_BOT_USERNAME)
   - Устанавливаются в bot_controller.py при запуске бота
   - Не забудьте их устанавливать если добавляете нового бота

---

## 📚 Полезные Ссылки в Проекте

- **Полная архитектура**: `/ARCHITECTURE.md` (ты здесь создал)
- **Инструкция по миграции**: `/migration_guide.md`
- **README**: `/README.md` (общая информация)
- **Версия приложения**: `src/shop_bot/version.py`

---

## 🔧 Типичные Задачи

### Добавить новую конфигурацию

1. Добавьте в `default_settings` в `database.py`
2. Прочитайте через `get_setting("your_key")`
3. Обновите в админ-панели (не забудьте HTML)

### Добавить новую команду бота

1. В `handlers.py` создайте новый обработчик
2. Зарегистрируйте через `@router.message(Command("command_name"))`
3. Добавьте кнопку в `keyboards.py` если нужна

### Добавить новый платеж метод

1. Посмотрите как реализован YooKassa в `handlers.py`
2. Создайте аналогичный обработчик для нового метода
3. Добавьте вебхук endpoint в `app.py`
4. Добавьте конфигурацию в БД и админ-панель

---

## ⏱️ Типичное Время Выполнения Операций

| Операция | Время |
|---|---|
| Регистрация пользователя | < 1 сек |
| Создание VPN ключа | 2-5 сек |
| Проверка подписок (для 1000 пользователей) | ~ 30 сек |
| Создание резервной копии БД | 2-10 сек |
| Обновление проекта из GitHub | 30-60 сек |
| Запуск контейнера | 5-10 сек |

---

## 🎓 Учебные Примеры

### Как получить все ключи пользователя?

```python
from shop_bot.data_manager import database

user_id = 123456
keys = database.get_user_keys(user_id)
for key in keys:
    print(f"Ключ на {key['host_name']}: {key['key_email']}")
```

### Как получить статистику?

```python
from shop_bot.data_manager import database

total_users = database.get_user_count()
total_spent = database.get_total_spent_sum()
total_keys = database.get_total_keys_count()
print(f"Пользователей: {total_users}, Всего потрачено: {total_spent}, Ключей: {total_keys}")
```

### Как отправить сообщение пользователю из фонового процесса?

```python
from aiogram import Bot

async def send_notification(bot: Bot, user_id: int, message_text: str):
    try:
        await bot.send_message(chat_id=user_id, text=message_text)
    except Exception as e:
        logger.error(f"Failed to send message to {user_id}: {e}")
```

---

**Создано**: 5 февраля 2026  
**Версия проекта**: 2.3.7
