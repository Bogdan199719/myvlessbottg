# 📊 Анализ Критичных Данных и Точек Отказа

> Документ описывает где находятся самые важные данные в проекте и что произойдет при их потере.

---

## 🗂️ Самые Важные Файлы и Данные

### ⭐⭐⭐ КРИТИЧНОСТЬ: МАКСИМУМ

#### 1. **users.db** (SQLite База Данных)
**Местоположение**: `/path/to/vless-shopbot/users.db`

**Содержит**:
- ✅ **Все пользователи** (telegram_id, username, балансы)
- ✅ **Все VPN ключи** (какие ключи выданы, сроки действия)
- ✅ **Все платежи** (история транзакций, сумма, статус)
- ✅ **Конфигурация** (токены, пароли, настройки)
- ✅ **Поддержка** (тикеты, истории обращений)
- ✅ **Хосты и тарифы** (конфигурация серверов и цены)

**При потере**:
- ❌ Потеря всех данных пользователей
- ❌ Невозможно восстановить выданные ключи
- ❌ Потеря истории платежей
- ❌ Потеря конфигурации системы
- ⚠️ **КРИТИЧНАЯ ПОТЕРЯ ДЕНЕГ** (история платежей)

**Размер**: Обычно 10-500 MB в зависимости от количества пользователей

**Восстановление**: Есть резервная копия на локальном ПК или на сервере (если настроены регулярные backups)

---

#### 2. **.env** (Конфигурация и Токены)
**Местоположение**: `/path/to/vless-shopbot/.env`

**Содержит**:
- 🔑 **TELEGRAM_BOT_TOKEN** - токен основного бота
- 🔑 **SUPPORT_BOT_TOKEN** - токен бота поддержки  
- 🔑 **YOOKASSA_SHOP_ID и SECRET_KEY** - доступ к платежной системе
- 🔑 **CRYPTOBOT_TOKEN** - доступ к крипто-платежам
- 🔑 **HELEKET API KEY** - доступ к платежам
- 🔑 **TON_WALLET_ADDRESS и TONAPI_KEY** - крипто кошелек
- 🔑 **ADMIN_TELEGRAM_ID** - ID администратора
- 📝 **DOMAIN** - домен сервера
- 📝 **PANEL_LOGIN и PANEL_PASSWORD** - пароли админ-панели

**При потере**:
- ❌ Бот не сможет подключиться к Telegram
- ❌ Платежи не будут работать
- ❌ Админ-панель не будет доступна
- ⚠️ **Все секреты скомпрометированы** (нужно менять все ключи)

**Размер**: Обычно 1-2 KB

**Восстановление**: Нет автоматических резервных копий! Нужно хранить отдельно и безопасно

**Рекомендация**: 
- ✅ Хранить на защищенном сервере резервных копий
- ✅ Не коммитить в Git
- ✅ Использовать `chmod 600` для защиты

---

### ⭐⭐ КРИТИЧНОСТЬ: ВЫСОКАЯ

#### 3. **Исходный Код** (папка `src/`)
**Местоположение**: `src/shop_bot/`

**Содержит**:
- 📝 Всю логику бота (~2700 строк в handlers.py)
- 📝 Управление БД (~1300 строк в database.py)
- 📝 Интеграцию с 3x-ui (~600 строк в xui_api.py)
- 📝 Админ-панель (~1300 строк в app.py)
- 📝 Планировщик уведомлений (~370 строк в scheduler.py)

**При потере**:
- ❌ Невозможно развернуть новую версию
- ❌ Потеря всех сделанных улучшений
- ⚠️ Нужно восстанавливать с GitHub

**Размер**: Обычно 50-100 KB

**Восстановление**: GitHub репозиторий есть, но может быть старой версией

---

#### 4. **Dockerfile и docker-compose.yml**
**Местоположение**: `/path/to/vless-shopbot/Dockerfile` и `docker-compose.yml`

**При потере**:
- ❌ Невозможно пересобрать контейнер
- ⚠️ Нужно восстанавливать с GitHub или из памяти

**Совет**: Эти файлы редко меняются, но всегда должны быть в Git

---

### ⭐ КРИТИЧНОСТЬ: СРЕДНЯЯ

#### 5. **pyproject.toml** (Зависимости)
**Местоположение**: `/path/to/vless-shopbot/pyproject.toml`

**При потере**:
- ⚠️ Не удастся переустановить зависимости
- Нужно восстанавливать с GitHub

#### 6. **Лог-файлы** (для отладки)
**Местоположение**: `/var/log/` или в Docker контейнере

**Размер**: Может быть очень большим (100+ MB)

**Рекомендация**: Регулярно ротировать логи

---

## 🔄 Жизненный Цикл Критичных Данных

```
┌─────────────────────────────────────┐
│  Пользователь платит в Telegram     │
└────────────────┬────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│  Платежная система отправляет       │
│  вебхук на наш сервер               │
└────────────────┬────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│  handlers.py обрабатывает вебхук    │
│  (КРИТИЧНАЯ ТОЧКА 1)                │
└────────────────┬────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│  xui_api.py создает ключ на хосте   │
│  (КРИТИЧНАЯ ТОЧКА 2)                │
└────────────────┬────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│  database.py записывает в users.db  │
│  (КРИТИЧНАЯ ТОЧКА 3)                │
└────────────────┬────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│  handlers.py отправляет ключ юзеру  │
│  (КРИТИЧНАЯ ТОЧКА 4)                │
└─────────────────────────────────────┘
```

**Если процесс прервется**:
- **На пункте 2**: Ключ на хосте создан, но юзер не знает о нем
- **На пункте 3**: Платеж потерян (деньги приняты, ключ не выдан)
- **На пункте 4**: Ключ есть в БД, но юзер не получит сообщение

---

## 💾 Требования к Резервному Копированию

### Какие Файлы Резервировать

| Файл | Частота | Где хранить | Проверка |
|---|---|---|---|
| `users.db` | Каждый день | ✅ На диск + облако | Раз в неделю пытаться восстановить |
| `.env` | При изменении | ✅ На защищенном сервере | Никогда не коммитить в Git |
| Исходный код | Каждый commit | ✅ В GitHub репо | Уже в контроле версий |
| Логи | Не нужно | ❌ Удалять старые | Максимум 30 дней |

### Процесс Резервного Копирования

**Ежедневный автоматический backup** (2:00 AM):
```bash
0 2 * * * cd /path/to/vless-shopbot && cp users.db users.db.backup.$(date +\%Y\%m\%d)
```

**Еженедельная проверка**:
```bash
# Каждый понедельник в 03:00
0 3 * * 1 sqlite3 /path/to/vless-shopbot/users.db.backup.* "PRAGMA integrity_check;"
```

**Ежемесячная отправка в облако** (1-го числа в 04:00):
```bash
0 4 1 * * scp /path/to/vless-shopbot/users.db.backup.* \
    backup_user@backup_server:/backups/vlesshop/
```

---

## 🚨 Сценарии Отказа и Восстановление

### Сценарий 1: БД повреждена (Database Corruption)

**Как узнать**:
```bash
sqlite3 users.db "PRAGMA integrity_check;"
# Вернет: "ok" или ошибка типа "Error: database disk image is malformed"
```

**Как восстановить**:
1. Остановите контейнер: `docker-compose down`
2. Восстановите из последней резервной копии: `cp users.db.backup.20260105 users.db`
3. Проверьте: `sqlite3 users.db "SELECT COUNT(*) FROM users;"`
4. Запустите: `docker-compose up -d`

**Время восстановления**: 5-10 минут

---

### Сценарий 2: .env файл потеря/взлом

**Если потеря (забыли что там было)**:
1. ✅ Восстановите из облака
2. Если облака нет, придется перегенерировать все токены:
   - Новый токен бота (попросить у @BotFather)
   - Новые ключи платежных систем
   - Новый пароль админ-панели

**Если взлом (кто-то получил .env)**:
1. ⚠️ **КРИТИЧНО!** Немедленно смените все токены
2. Возьмите новый токен бота у @BotFather
3. Сгенерируйте новые API ключи для платежных систем
4. Отмените скомпрометированные токены на сервисах
5. Обновите `.env` с новыми значениями
6. Перезагрузите контейнер

**Время восстановления**: 30 минут (зависит от платежных систем)

---

### Сценарий 3: Вся папка проекта удалена

**Как восстановить**:
1. Восстановите исходный код из GitHub: `git clone ...`
2. Восстановите `.env` из облачного backup
3. Восстановите `users.db` из последней резервной копии
4. Запустите: `docker-compose up -d --build`

**Время восстановления**: 15-30 минут

---

### Сценарий 4: Контейнер упал и не перезапускается

**Диагностика**:
```bash
docker ps -a | grep bot  # Посмотреть статус
docker logs <container_id>  # Посмотреть ошибку
```

**Решение**:
```bash
# Удалить сломанный контейнер
docker rm <container_id>

# Пересобрать
docker-compose up -d --build

# Проверить
docker logs -f <container_name>
```

**Время восстановления**: 10 минут

---

### Сценарий 5: Сервер полностью упал

**Если есть резервная копия**:
1. Восстановитесь с нуля на новом сервере
2. Установите необходимое ПО (Docker, git)
3. Скачайте исходный код
4. Восстановите `.env` и `users.db`
5. Запустите `docker-compose up -d`

**Время восстановления**: 30-60 минут

**Если резервной копии нет**:
- ❌ Потеря всех данных пользователей
- ❌ Потеря истории платежей
- ⚠️ **Финансовые потери**

**Вывод**: ВСЕГДА ДЕЛАЙТЕ РЕЗЕРВНЫЕ КОПИИ!

---

## 📈 Мониторинг Критичных Точек

### Что Мониторить

1. **Доступность БД**:
   ```bash
   # Каждый час проверяйте что БД работает
   0 * * * * sqlite3 /path/to/vless-shopbot/users.db "SELECT COUNT(*) FROM users;"
   ```

2. **Размер БД**:
   ```bash
   # Если размер > 500 MB, нужна оптимизация
   ls -lh /path/to/vless-shopbot/users.db
   ```

3. **Процесс бота**:
   ```bash
   # Проверяйте что контейнер запущен
   docker ps | grep bot
   ```

4. **Свободное место на диске**:
   ```bash
   # Должно быть минимум 1 GB свободно
   df -h | grep -E "/$|/var"
   ```

5. **Логи ошибок**:
   ```bash
   # Проверяйте ошибки в реальном времени
   docker logs -f <container_name> | grep -i error
   ```

---

## 🛡️ Рекомендации по Защите

### Безопасность .env
```bash
# На сервере установите права
chmod 600 /path/to/vless-shopbot/.env

# Проверьте что только владелец может читать
ls -la /path/to/vless-shopbot/.env
# -rw------- 1 user user 1024 Jan 5 10:00 .env
```

### Безопасность Резервной Копии
```bash
# Резервные копии должны быть защищены
chmod 600 /path/to/vless-shopbot/users.db.backup.*

# Передача по сети (используйте SCP с ключами SSH)
scp -i ~/.ssh/id_rsa users.db backup@server:/backups/

# Хранение в облаке (используйте шифрование)
# Например: Backblaze, AWS S3 с шифрованием
```

### Контроль Доступа
```bash
# Только владелец может видеть папку проекта
chmod 700 /path/to/vless-shopbot

# Логи должны быть доступны только корню
chmod 644 /path/to/vless-shopbot/*.log
```

---

## 📋 Чек-лист для Production

- [ ] **Резервная копия БД** автоматизирована (каждый день)
- [ ] **Резервная копия .env** хранится защищено (облако или зашифровано)
- [ ] **Проверка целостности БД** автоматизирована (каждую неделю)
- [ ] **Монитор доступности** контейнера (каждый час)
- [ ] **Алертинг** при ошибках (выслать email админу)
- [ ] **Процедура восстановления** документирована
- [ ] **Тестирование восстановления** выполнено (раз в месяц)
- [ ] **Права доступа** правильно установлены
- [ ] **Шифрование** для чувствительных данных
- [ ] **Логирование** всех критичных операций

---

## 📞 При Катастрофе

### Если Потеряли Данные

1. **Не паникуйте** - обычно есть восстановление
2. **Остановите бот**: `docker-compose down`
3. **Восстановите из резервной копии**: `cp users.db.backup users.db`
4. **Проверьте целостность**: `sqlite3 users.db "PRAGMA integrity_check;"`
5. **Запустите снова**: `docker-compose up -d`
6. **Проверьте логи**: `docker logs -f`

### Если Нет Резервной Копии

1. **Проверьте Google Drive, Dropbox, облако**
2. **Проверьте на локальном ПК** где вы редактировали код
3. **Проверьте на другом сервере** если была синхронизация
4. **Спросите у коллег** может они тоже делали копию
5. **Последнее средство**: восстановление файлов утилитой `extundelete`

---

## 🎓 Обучение Команды

Убедитесь что вся команда знает:

1. **Где находятся критичные данные**
2. **Как восстанавливать из резервной копии**
3. **Кого звать при катастрофе**
4. **Как делать резервные копии вручную**
5. **Как проверять целостность БД**

---

**Последнее обновление**: 5 февраля 2026  
**Версия документа**: 1.0  
**Статус**: ✅ Актуально для v2.3.7
