# ⚡ Краткий Справочник Команд и Процедур\n\n> Вся нужная информация одна страница для быстрого доступа.\n\n---\n\n## 🚀 Развертывание на Сервере\n\n### Основной Процесс (Только src/ изменился)\n```bash\n# На вашем ПК\nscp -r src/ user@server:/path/to/vless-shopbot/\n\n# На сервере\nssh user@server\ncd /path/to/vless-shopbot\ndocker-compose down\ndocker-compose up -d\ndocker logs -f <container_name>\n```\n\n### Если Изменились Зависимости (pyproject.toml)\n```bash\nscp pyproject.toml user@server:/path/to/vless-shopbot/\nssh user@server\ncd /path/to/vless-shopbot\ndocker-compose down\ndocker-compose up -d --build\n```\n\n### Если Изменились Docker Файлы\n```bash\nscp docker-compose.yml Dockerfile user@server:/path/to/vless-shopbot/\nssh user@server\ncd /path/to/vless-shopbot\ndocker-compose down\ndocker-compose up -d --build\n```\n\n---\n\n## 🔍 Отладка\n\n### Посмотреть Логи\n```bash\n# Последние 100 строк\ndocker logs --tail 100 <container_name>\n\n# В реальном времени\ndocker logs -f <container_name>\n\n# Поиск ошибок\ndocker logs <container_name> | grep -i error\n```\n\n### Проверить БД\n```bash\n# Заход в SQLite\nssh user@server\ndocker exec -it <container_name> sqlite3 users.db\n\n# Внутри sqlite3:\nSELECT COUNT(*) FROM users;  -- Сколько пользователей\nSELECT * FROM users WHERE telegram_id=123456;  -- Юзер\nSELECT * FROM transactions ORDER BY transaction_id DESC LIMIT 10;  -- Платежи\nPRAGMA integrity_check;  -- Проверить целостность\n.exit  -- Выход\n```\n\n### Проверить Контейнер\n```bash\n# Запущен ли?\ndocker ps | grep bot\n\n# Порты\ndocker ps --format \"table {{.Names}}\\t{{.Ports}}\"\n\n# Статус\ndocker inspect <container_name> | grep Status\n```\n\n---\n\n## 💾 Резервное Копирование\n\n### Создать Резервную Копию\n```bash\nssh user@server\ncd /path/to/vless-shopbot\ndocker-compose down  # Важно!\ncp users.db users.db.backup.$(date +%Y%m%d_%H%M%S)\nscp users.db.backup.* ~/backups/  # Скачать на свой ПК\ndocker-compose up -d\n```\n\n### Восстановить из Резервной Копии\n```bash\nssh user@server\ncd /path/to/vless-shopbot\ndocker-compose down\ncp users.db.backup.20260105_103045 users.db  # Выбрать нужную дату\nddocker-compose up -d\n```\n\n---\n\n## 📊 Быстрые Команды на Сервере\n\n```bash\n# Рестарт контейнера\ndocker-compose restart\n\n# Полная пересборка\ndocker-compose down && docker-compose up -d --build\n\n# Удалить старые образы (освободить место)\ndocker image prune -a\n\n# Посмотреть размер БД\nls -lh users.db\n\n# Проверить свободное место\ndf -h\n\n# Статус сервиса\nsudo systemctl status docker\n\n# Логирование Docker\njournalctl -u docker -f\n```\n\n---\n\n## 🔧 Быстрое Добавление Функции\n\n### Новая Команда Бота\n1. Отредактируйте `src/shop_bot/bot/handlers.py`\n2. Добавьте обработчик:\n```python\n@router.message(Command(\"new_command\"))\nasync def handle_new_command(message: types.Message):\n    await message.answer(\"Hello!\")\n```\n3. Скопируйте на сервер\n4. Перезагрузитесь\n\n### Новая Конфигурация\n1. В `src/shop_bot/data_manager/database.py` добавьте в `default_settings`\n2. Используйте: `get_setting(\"your_key\")`\n3. Добавьте в админ-панель (templates)\n\n---\n\n## 🚨 Критичные Файлы (НИКОГДА НЕ ТЕРЯЙТЕ!)\n\n```\n💾 users.db              ← БАЗА ДАННЫХ (главное!!!)\n🔑 .env                  ← КОНФИГ И ТОКЕНЫ\n📝 src/                  ← ИСХОДНЫЙ КОД\n🐳 docker-compose.yml    ← DOCKER КОНФИГ\n🐳 Dockerfile            ← DOCKER ОБРАЗ\n```\n\n---\n\n## 📋 Файлы для Редактирования\n\n| Что менять | Файл | Пересборка? |\n|---|---|---|\n| Команды бота | `src/shop_bot/bot/handlers.py` | Нет |\n| Конфигурация | `src/shop_bot/data_manager/database.py` | Нет |\n| Админ-панель | `src/shop_bot/webhook_server/app.py` | Нет |\n| Дизайн панели | `src/shop_bot/webhook_server/templates/` | Нет |\n| Зависимости | `pyproject.toml` | ✅ Да |\n| Docker | `Dockerfile` | ✅ Да |\n| Docker конфиг | `docker-compose.yml` | ✅ Да |\n| Окружение | `.env` | Перезагрузка |\n\n---\n\n## 🔗 Основные URL в Проекте\n\n```\nАдмин-панель:     http://your-domain.com:1488/\nAPI для клиентов: http://your-domain.com:1488/sub/<token>\nТелеграм бот:     https://t.me/YourBotName\nWebhook платежей: http://your-domain.com:1488/pay/\n```\n\n---\n\n## 📞 Контакты и Ссылки\n\n```\nАвтор:    Bogdan199719\nEmail:    bogdan123456789101@gmail.com\nGitHub:   https://github.com/Bogdan199719/myvlessbottg\n```\n\n---\n\n## ✅ Перед Развертыванием\n\n- [ ] Сделал резервную копию\n- [ ] Проверил синтаксис Python файлов\n- [ ] Скопировал все нужные файлы\n- [ ] Перезагрузил контейнер\n- [ ] Проверил логи\n- [ ] Тестовый платеж прошел\n- [ ] Ключ выдан\n- [ ] Всё работает\n\n---\n\n## 🆘 Быстрое Решение Проблем\n\n### \"Бот не отвечает\"\n```bash\ndocker logs -f <container_name> | grep -i \"error\\|token\"\n# Проверить токен в админ-панели\n```\n\n### \"Ключ не выдан\"\n```bash\n# Проверить платеж\nsqlite3 users.db \"SELECT * FROM transactions WHERE payment_id='XXX';\"\n# Вручную переиздать через админ-панель\n```\n\n### \"БД заблокирована\"\n```bash\ndocker-compose down\nsleep 5\ndocker-compose up -d\n```\n\n### \"Контейнер не запускается\"\n```bash\ndocker-compose down\ndocker-compose up -d --build\ndocker logs -f\n```\n\n---\n\n## 🎯 Типичный День\n\n### Утро\n```bash\n# Проверить что всё запущено\nssh user@server\ndocker ps | grep bot\n```\n\n### Во время разработки\n```bash\n# Отредактируйте файлы локально\n# Скопируйте на сервер\nscp -r src/ user@server:/path/to/vless-shopbot/\n# Перезагрузитесь\nssh user@server\ncd /path/to/vless-shopbot\ndocker-compose restart\n# Проверьте логи\ndocker logs -f\n```\n\n### Вечер\n```bash\n# Сделайте резервную копию\ncd /path/to/vless-shopbot\ncp users.db users.db.backup.$(date +%Y%m%d)\n# Убедитесь что всё стабильно\ndocker logs --tail 50\n```\n\n---\n\n**Последнее обновление**: 5 февраля 2026\n